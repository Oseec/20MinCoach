/* export type AppEvents =
  | { type: "session:created"; payload: { id: string; when: string } }
  | { type: "session:started"; payload: { id: string } }
  | { type: "coach:presence"; payload: { coachId: string; online: boolean } }
  | { type: "notif:new"; payload: { title: string; body: string } };
 */